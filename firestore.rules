rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access, but only content owners can write
    match /items/{document} {
      allow read: if true
      allow write: if request.auth.uid == 'u2rpGcGoamVWw5uS6U4JZx1IrmE2';
    }
  }
}